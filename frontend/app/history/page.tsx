'use client'
import {useEffect,useState} from 'react';import Link from 'next/link';import AppShell from '../../components/AppShell';import {getHistory} from '../../lib/api'
export default function History(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{getHistory().then(setRows)},[]);return <AppShell><div className="pageTitle"><div><h1>Your Analysis History</h1><p>Review previous resume scans and job matches.</p></div></div><section className="panel historyPanel">{rows.length?rows.map(x=><div className="historyRow" key={x.id}><div className="docIcon">▤</div><div className="historyInfo"><b>{x.result?.target_title||'Resume Evidence Analysis'}{x.result?.target_company?` - ${x.result.target_company}`:''}</b><span>{new Date(x.created_at).toLocaleDateString()} · {x.result?.resume_filename}</span></div><div className="miniScore">{Math.round(x.overall_score)}%</div><Link className="outline linkBtn" href={`/results/${x.id}`}>View</Link></div>):<div className="empty"><b>No analyses yet.</b><p>Analyze your first resume to build your history.</p><Link className="primary linkBtn" href="/dashboard">Analyze Resume</Link></div>}</section></AppShell>}
