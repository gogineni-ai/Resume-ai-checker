'use client'
import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import Logo from '../../components/Logo';import {register,saveSession} from '../../lib/api'
export default function Signup(){const r=useRouter();const [name,setName]=useState(''),[email,setEmail]=useState(''),[phone,setPhone]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false)
 async function submit(e:any){e.preventDefault();setBusy(true);setErr('');try{const d=await register({name,email,phone,password});saveSession(d);r.push('/dashboard')}catch(x:any){setErr(x.message)}finally{setBusy(false)}}
 return <div className="authPage soft"><header className="publicHeader"><Logo/><nav><Link href="/login">Login</Link><Link className="primarySmall" href="/signup">Sign Up</Link></nav></header><div className="signupWrap"><form className="signupCard" onSubmit={submit}><div className="miniLogo"><div className="brandMark">R<span>✓</span></div></div><h1>Create Your Account</h1><p>Join professionals who verify their resumes with confidence</p><label>Full Name<div className="field"><span>♙</span><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Ravi Sankar Gogineni"/></div></label><label>Email Address<div className="field"><span>✉</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div></label><label>Phone Number<div className="field"><span>☎</span><input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="(469) 123-4567"/></div></label><label>Password<div className="field"><span>▣</span><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimum 8 characters"/></div></label><small className="hint">Password must be at least 8 characters</small>{err&&<div className="formError">{err}</div>}<button className="primary" disabled={busy}>{busy?'Creating account…':'Create Account'}</button><p className="terms">By creating an account, you agree to our <a>Terms of Service</a> and <a>Privacy Policy</a>.</p><p className="authBottom">Already have an account? <Link href="/login">Login</Link></p></form></div></div>}
