import './styles.css'
export const metadata={title:'Resume Verifier AI',description:'Evidence-based resume and job-description analysis'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
